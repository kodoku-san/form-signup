const formMain = document.querySelector('#form-signup'),
    input = formMain.querySelectorAll('.signup-form-group__input'),
    label = formMain.querySelectorAll('.signup-form-group__label')


input.forEach((element, index) => {
    
});